name: Scrape & Filter Proxies

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: walla-proxy-scrape
  cancel-in-progress: true

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: GeoLite cache key
        id: geo-date
        run: echo "week=$(date -u +%G-%V)" >> "$GITHUB_OUTPUT"

      - name: Cache GeoLite2 Country
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/GeoLite2-Country.mmdb
          key: geolite2-country-${{ runner.os }}-${{ steps.geo-date.outputs.week }}

      - name: Install dependencies
        run: pip install aiohttp aiohttp-socks geoip2

      - name: Run v2 proxy engine with MaxMind/IPinfo geolocation
        env:
          MAXMIND_ACCOUNT_ID: ${{ secrets.MAXMIND_ACCOUNT_ID }}
          MAXMIND_LICENSE_KEY: ${{ secrets.MAXMIND_LICENSE_KEY }}
          IPINFO_TOKEN: ${{ secrets.IPINFO_TOKEN }}
        run: python geo_runner.py scripts/proxy-aggregator.py

      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          for i in {1..3}; do
            git add -A
            git diff --cached --quiet || git commit -m "Update proxy lists v2 [skip ci]"
            git pull --rebase --autostash origin main && git push && break
            echo "Push failed (attempt $i/3), retrying in 10s..."
            sleep 10
          done
